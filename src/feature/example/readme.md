This folder serves as an example of file organization under the feature directory.
Follow these rules:

1. Use singular forms, e.g., use `hook`, not `hooks`
2. When `type` and `util` have little content, you can create files only, no need to create folders
